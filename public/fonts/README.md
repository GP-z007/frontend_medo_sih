# UDAAN webfonts

Served from this directory:

- `Absans-Regular.woff2` / `.woff` — UI & display (Collletttivo Absans, OFL)
- `IBMPlexMono-*.ttf` — metrics, codes, tables (IBM Plex Mono, OFL)

Source copies live in `/fonts` at the repo root.
